<p>
    <b><%= AuthenticatedSystem::Role.human_attribute_name(:title).s %></b><br />
<%= f.text_field :title %>
</p>
<p>
    <b><%= AuthenticatedSystem::Role.human_attribute_name(:description).s %></b><br />
<%= f.text_area :description, :rows => 5 %>
</p>
  <p><b><%= AuthenticatedSystem::Permission.model_name.human(:count => :many).titleize.s %></b></p>
<%=  render :partial => 'shared/double_select', :locals => { :available_options_title => "#{t(:available, :what => AuthenticatedSystem::Permission.model_name.human(:count => :many))}:",
     :associated_options_title => "#{t('associate.d.record', :what => AuthenticatedSystem::Permission.model_name.human(:count => :many))}:",
     :associated_options => @role.permissions.collect { |p| [p.title, p.id]},
     :all_options => @permissions.collect { |p| [p.title, p.id]} }%>