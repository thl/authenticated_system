<%  if !@person.user.nil?
      authorized_only hash_for_edit_person_user_path do %>
<%=     link_to ts('edit.record', :what => User.model_name.human), edit_person_user_path(@person) %> |
<%    end
    end
    authorized_only hash_for_person_path(:action => 'destroy') do %>
<%=   link_to ts('delete.this'), person_path(@person), :confirm => te('delete.confirmation'), :method => :delete %> |
<%  end %>
<%= link_to ts('back.to', :what => t('all.records', :what => Person.model_name.human(:count => :many))), people_path %> |
<%= link_to ts('back.to', :what => t('home.this')), root_path %>